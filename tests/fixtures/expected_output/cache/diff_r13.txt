Index: include/utils.h
===================================================================
--- include/utils.h	(revision 12)
+++ include/utils.h	(nonexistent)
@@ -1,18 +0,0 @@
-﻿#ifndef UTILS_H
-#define UTILS_H
-
-#include <string>
-
-namespace ninja {
-namespace utils {
-
-std::string trim(const std::string& str);
-bool isNumeric(const std::string& str);
-std::string toLower(const std::string& str);
-void printBanner();
-void printHelp();
-
-} // namespace utils
-} // namespace ninja
-
-#endif // UTILS_H
\ No newline at end of file
Index: include/string_utils.h
===================================================================
--- include/string_utils.h	(nonexistent)
+++ include/string_utils.h	(revision 13)
@@ -0,0 +1,18 @@
+﻿#ifndef STRING_UTILS_H
+#define STRING_UTILS_H
+
+#include <string>
+
+namespace ninja {
+namespace utils {
+
+std::string trim(const std::string& str);
+bool isNumeric(const std::string& str);
+std::string toLower(const std::string& str);
+void printBanner();
+void printHelp();
+
+} // namespace utils
+} // namespace ninja
+
+#endif // STRING_UTILS_H
\ No newline at end of file
Index: src/utils.cpp
===================================================================
--- src/utils.cpp	(revision 12)
+++ src/utils.cpp	(nonexistent)
@@ -1,51 +0,0 @@
-﻿#include "utils.h"
-#include <algorithm>
-#include <iostream>
-#include <cctype>
-#include <sstream>
-
-namespace ninja {
-namespace utils {
-
-std::string trim(const std::string& str) {
-    if (str.empty()) return str;
-    auto start = std::find_if(str.begin(), str.end(),
-        [](unsigned char c) { return !std::isspace(c); });
-    auto end = std::find_if(str.rbegin(), str.rend(),
-        [](unsigned char c) { return !std::isspace(c); }).base();
-    return (start < end) ? std::string(start, end) : std::string();
-}
-
-bool isNumeric(const std::string& str) {
-    if (str.empty()) return false;
-    std::istringstream iss(str);
-    double d;
-    iss >> std::noskipws >> d;
-    return iss.eof() && !iss.fail();
-}
-
-std::string toLower(const std::string& str) {
-    std::string result = str;
-    std::transform(result.begin(), result.end(), result.begin(),
-        [](unsigned char c) { return std::tolower(c); });
-    return result;
-}
-
-void printBanner() {
-    std::cout << "+================================+" << std::endl;
-    std::cout << "|       NinjaCalc v1.1           |" << std::endl;
-    std::cout << "|   Type 'quit' to exit          |" << std::endl;
-    std::cout << "|   Type 'help' for commands     |" << std::endl;
-    std::cout << "+================================+" << std::endl;
-}
-
-void printHelp() {
-    std::cout << "Commands:" << std::endl;
-    std::cout << "  <expression>  Evaluate math expression" << std::endl;
-    std::cout << "  help          Show this help" << std::endl;
-    std::cout << "  history       Show calculation history" << std::endl;
-    std::cout << "  quit/exit     Exit the program" << std::endl;
-}
-
-} // namespace utils
-} // namespace ninja
\ No newline at end of file
Index: src/main.cpp
===================================================================
--- src/main.cpp	(revision 12)
+++ src/main.cpp	(revision 13)
@@ -1,5 +1,5 @@
 ﻿#include "calculator.h"
-#include "utils.h"
+#include "string_utils.h"
 #include <iostream>
 #include <string>
 
Index: src/string_utils.cpp
===================================================================
--- src/string_utils.cpp	(nonexistent)
+++ src/string_utils.cpp	(revision 13)
@@ -0,0 +1,51 @@
+﻿#include "string_utils.h"
+#include <algorithm>
+#include <iostream>
+#include <cctype>
+#include <sstream>
+
+namespace ninja {
+namespace utils {
+
+std::string trim(const std::string& str) {
+    if (str.empty()) return str;
+    auto start = std::find_if(str.begin(), str.end(),
+        [](unsigned char c) { return !std::isspace(c); });
+    auto end = std::find_if(str.rbegin(), str.rend(),
+        [](unsigned char c) { return !std::isspace(c); }).base();
+    return (start < end) ? std::string(start, end) : std::string();
+}
+
+bool isNumeric(const std::string& str) {
+    if (str.empty()) return false;
+    std::istringstream iss(str);
+    double d;
+    iss >> std::noskipws >> d;
+    return iss.eof() && !iss.fail();
+}
+
+std::string toLower(const std::string& str) {
+    std::string result = str;
+    std::transform(result.begin(), result.end(), result.begin(),
+        [](unsigned char c) { return std::tolower(c); });
+    return result;
+}
+
+void printBanner() {
+    std::cout << "+================================+" << std::endl;
+    std::cout << "|       NinjaCalc v1.1           |" << std::endl;
+    std::cout << "|   Type 'quit' to exit          |" << std::endl;
+    std::cout << "|   Type 'help' for commands     |" << std::endl;
+    std::cout << "+================================+" << std::endl;
+}
+
+void printHelp() {
+    std::cout << "Commands:" << std::endl;
+    std::cout << "  <expression>  Evaluate math expression" << std::endl;
+    std::cout << "  help          Show this help" << std::endl;
+    std::cout << "  history       Show calculation history" << std::endl;
+    std::cout << "  quit/exit     Exit the program" << std::endl;
+}
+
+} // namespace utils
+} // namespace ninja
\ No newline at end of file

